---
source: 同花顺
Holding:
  - 是
---
 观点汇总
```dataview
TABLE 
  regexreplace(file.name, ".*--", "") as 观点,
  split(file.name, "--")[1] as 作者,
  split(file.name, "--")[2] as 日期
WHERE contains(file.name, "卫星化学")
WHERE file.name != this.file.name
SORT 日期 DESC
```
     

